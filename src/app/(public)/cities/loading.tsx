import { LoadingContent } from "@/components/public/recovery-content";

// Stream only always-existing listing routes, not the city/venue existence gates.
export default function CitiesLoading() { return <LoadingContent />; }