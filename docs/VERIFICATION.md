# Shagun — verification record

**Evidence cutoff: 2026-09-11 UTC; runs span 8–11 September.** Backup capture/verification dates below use their actual UTC receipts, not an inferred browser date or total session duration. Local tests, managed SQL/HTTP checks, review publication and hosted CI have distinct environment/revision boundaries. A passing result applies only to its stated environment and scope.

## 1,000-row imports and simplified city creation - 2026-09-11 UTC

Application/test source [00c5c35ace5b1ec993392c6cc009c085621d0157](https://github.com/RichardHenryJames/shagun/commit/00c5c35ace5b1ec993392c6cc009c085621d0157) is published on **`audit/second-pass-2026-09-09`** and deployed to the short admin Preview. It supersedes the 100-row limit and long new-city form in the earlier release below. No migration, hosted inventory write, permission change or Production promotion was performed for this update.

| Check | Actual result and scope |
| --- | --- |
| Local quality | **PASS:** final `npm run check`, lint, route generation, strict TypeScript and **1,252 tests across 19 files**. Includes **108 import**, **130 action** and **96 catalog** tests: exact 1,000/1,001 boundaries, ten confirmed batches, bounded slug lookups, all-before-write validation, authorization renewal, partial-result merging, and automatic creation/search cases for all **36 represented Indian states/territories**. The existing catalog is not an exhaustive census. |
| Full-size local workflow | **1/1 passed in 12.1 minutes** at the 390 px project: the real UI created a draft city from selection alone, imported **1,000 drafts in ten requests**, checked field mapping, draft/unverified/unreviewed state, and exact saved-note/timestamp preservation across retry. Empty/one/many/result layouts passed at **320, 375, 390, 414, 768, 1024, 1440 and 1920 px**. Guarded cleanup used four browser tabs with the same real local session and UI deletion forms, not service-role inventory deletion. All 13 workflow steps completed. This run preceded the final city-only ambiguity/conflict feedback refinement; the import code was unchanged afterward. |
| Final tablet/desktop | **2/2 passed in 4.1 minutes** at **768/1440 px**, with the final city refinement and matching integration build. Each completed the simplified creation, two-draft import and all Auth/media/lifecycle/cleanup steps. The earlier attempt passed both new feature steps but failed later with **ECONNRESET** on public suggestions; desktop did not run in that attempt. The same command passed when repeated alone after the parallel quality job completed. No timeout/assertion/Auth changes were made for that failure, and concurrency is not asserted as a proven cause. |
| Hosted quality/browser | [Run 34647912713](https://github.com/RichardHenryJames/shagun/actions/runs/34647912713), exact source `00c5c35ace5b1ec993392c6cc009c085621d0157`: **Checks and isolated browser QA passed**, completed **21:13:01 UTC**. Its fresh public `many` scenario reported **186 passed / 1 intentional skip in 2.1 minutes**. Public `empty`/`one` scenarios were not newly rerun for this change; prior evidence remains historical. |
| Hosted real services | The same run's **Real local Auth, city publishing and Storage** job passed at **21:29:42 UTC**, with **3 workflows passed in 14.1 minutes** on the final committed code. Logs confirm the 1,000-draft phone import, two-draft tablet/desktop imports, all eight import layout widths and exact retry preservation. The job stopped its isolated stack. Hosted CI still uses disposable local services, not the shared managed database. |
| Fresh Preview | GitHub deployment **6401786099**, **Preview**, exact source `00c5c35ace5b1ec993392c6cc009c085621d0157`, completed **success at 21:09:53 UTC**. Provider [HScoPWQCu2j64dEpB3ojFib1SDcr](https://vercel.com/richards-projects-224a1dea/shagun/HScoPWQCu2j64dEpB3ojFib1SDcr), [unique URL](https://shagun-bkwas2map-richards-projects-224a1dea.vercel.app). The short [Add City page](https://shagun-admin.vercel.app/admin/cities/new) serves the new UI. Production domain/branch/configuration remain unchanged by this work. |
| Hosted read-only UI/template smoke | At **21:30-21:32 UTC**, the legitimate signed-in browser saw exactly **one visible new-city input**, no state/country/SEO/JSON inputs, and an enabled Create City command only after selection. Switching **Ahmedabad/Gujarat**, **Chennai/Tamil Nadu** and **Ranchi/Jharkhand** retained correct state/district and India, without a hidden previous-state filter. Selections were **not saved**. Import page **200** displayed **1,000 venues / 2 MiB**; template **200**, **68,992 bytes**, XLSX ZIP signature, private/no-store and noindex headers. Credential-omitted template access returned **401**. No synthetic workbook was submitted to hosted inventory. |
| Local process restoration | **PASS:** exact run-owned city/venue/media/account cleanup completed; only the dedicated integration services were stopped. The final ordinary production build passed, fixtures disabled; the existing local preview task is running at **http://localhost:3000**. Fresh normal-mode reads returned home **200**, suggestions **200/no-store** with empty items, and unconfigured import **503/private/no-store**. This preview is intentionally database-free. |

The user workflow is now **type city, select the correct state/district result, Create city**, then add/import venues from the saved workspace. Server-derived India/geography, available slugs and draft defaults require no extra entry; null SEO overrides use the existing automatic page title/description rather than invented content. Existing associated cities open without overwrite; ambiguous unassociated same-name records are not silently reused. Full saved-city editing remains available separately. File/expanded-byte safeguards, batch cancellation, explicit retry inspection and draft-only publication boundaries are documented in [OPERATIONS.md](OPERATIONS.md#excel-imports-for-a-saved-city) and [ARCHITECTURE.md](ARCHITECTURE.md#city-scoped-excel-import).

All results above belong to the named implementation revision. An evidence-only follow-up changes this record, not those test scopes; its own hosted CI/deployment status is not inferred from the implementation run. Managed 1,000-row write latency/concurrency, full recovery and actual photo rights remain separate checks, not certified by local/CI fixtures or read-only hosted smoke.

## City-scoped Excel import - 2026-09-11 UTC

The Excel feature and follow-up are published on **`audit/second-pass-2026-09-09`**, with application/test source [4fca99b572eef611f1efb1a6ff47ea5aa36fe48b](https://github.com/RichardHenryJames/shagun/commit/4fca99b572eef611f1efb1a6ff47ea5aa36fe48b). The existing user commit `410a0aa` was preserved unchanged. This is a review-branch release, not a new Production promotion or certification of managed bulk writes.

| Check | Actual result and scope |
| --- | --- |
| Local quality | **PASS:** `npm run check`, lint, route types, strict TypeScript and **1,148 tests in 19 files**, including **89 Excel contract/workbook/API tests**. This preceded the final test-only addition of 375/414 px; that addition passed the focused integration run and subsequent normal build's typecheck. Runtime dependency audit `npm audit --omit=dev` reported **0 vulnerabilities** on the committed lock. |
| Real local workflow | **3/3 passed in 4.6 minutes**, at **390/768/1440 px**, with real isolated Auth/REST/Storage and all **13 substeps** per workflow. The phone project imported **100** synthetic drafts; tablet/desktop each imported **2**. Tests covered all 29 fields, whole-workbook validation before writes, ordinary-user/anonymous denial, draft privacy, exact saved-note/timestamp preservation across retry and guarded UI cleanup. No hosted records or credentials were used. |
| Import layouts | After the full three-project pass, the final test-only viewport expansion passed the focused **390 px workflow in 3.1 minutes**. Empty, one-row, many-row and result states passed geometry/axe checks at **320, 375, 390, 414, 768, 1024, 1440 and 1920 px**, retaining the 100-row import and exact retry assertions. This additional run is not added to the three-workflow total; the earlier local tablet/desktop passes are not claimed as rerun after that test-only change. |
| Browser fixes | An early file selection could be missing from React state when hydration attached; the input callback ref now recovers its existing DOM file. A later note assertion did not distinguish save completion/form line endings from retry mutation. The final test uses the existing saved-version helper, verifies the initial edit with CRLF/LF input normalization, then compares the **raw persisted note and exact research/venue timestamps** unchanged across retry. No Auth bypass, assertion removal or timestamp normalization was introduced. |
| Local process restoration | **PASS:** owned integration record/media/account cleanup completed; `npm run integration:stop` stopped only that stack. A fresh normal production build passed with fixtures disabled, then **Shagun: Local app preview** reported ready at **http://localhost:3000**. Fresh reads returned home **200** with the city combobox, suggestions **200/no-store** with an empty `items` array, and the unconfigured private import API **503/private/no-store**. This is the normal no-database preview, not hosted inventory evidence. |
| Git push repair | A normal dry-run push reproduced **403**, identifying the wrong inherited GitHub CLI account. The approved owner's existing Git Credential Manager login passed; repository-local GitHub helper/username settings then made an ordinary dry run and real fast-forward push succeed. No global credential settings, commit author, user commit, `main`, secrets or sibling repository were changed. |
| Hosted CI | **PASS, both jobs:** [run 34623787298](https://github.com/RichardHenryJames/shagun/actions/runs/34623787298) is for exact source `4fca99b572eef611f1efb1a6ff47ea5aa36fe48b`. **Checks and isolated browser QA** completed successfully at **16:48:34 UTC**, and **Real local Auth, city publishing and Storage** at **16:54:56 UTC**. The latter ran the final committed three-project integration suite and stopped its owned stack. The older public `empty`/`one` fixture runs below are not new runs for this source; local totals are not substituted for fetched hosted test totals. |
| Fresh Preview deployment | GitHub deployment **6397650530**, source `4fca99b572eef611f1efb1a6ff47ea5aa36fe48b`, **Preview**, completed **success** at **2026-09-11T16:45:59Z**. Provider deployment [GBovGYbsqEYLUFQJkBxMs7NWPsRt](https://vercel.com/richards-projects-224a1dea/shagun/GBovGYbsqEYLUFQJkBxMs7NWPsRt), [unique URL](https://shagun-4gogsrgsj-richards-projects-224a1dea.vercel.app). The short admin hostname serves the new import page; no Production deployment or alias/configuration change was issued. |
| Hosted read-only smoke | The browser's legitimate signed-in session opened [Hazaribag Excel import](https://shagun-admin.vercel.app/admin/cities/hazaribag/import), showing the correct city and draft/unverified/review-pending states. Template GET returned **200**, XLSX content type, the city-named attachment, **16,443 bytes**, ZIP signature and **private/no-store/noindex** headers. Credential-omitted checks independently returned login **200** with email/password and no hosting redirect, template **401**, and empty same-origin import POST **401**, both private/no-store. No workbook or synthetic venue was submitted to hosted inventory; the template smoke does not certify managed 100-row writes, provider concurrency or recovery. |

The data-entry workflow and limits are in [OPERATIONS.md](OPERATIONS.md#excel-imports-for-a-saved-city). Full managed recovery, final-domain acceptance and actual photo rights remain independent requirements; the earlier public inventory observations below are not refreshed counts from this feature release. A later evidence-only commit does not change this tested source, but its hosted status must be checked separately rather than inferred from this run.

## Observed public inventory and missing photos — 2026-09-10 07:20–07:22 UTC

**Hazaribag is now publicly accessible on the peach site, with 12 published listings, but none has a saved venue photograph.** These fresh read-only observations supersede the earlier draft-only and old-Production 404 observations below. They do not retrospectively close the outstanding managed recovery/release requirements or establish a complete photographed Hazaribag guide. No publication, deployment, rollback, permission change or upload was performed in this image-check pass.

| Surface checked | Actual observation |
| --- | --- |
| Public city and venue details | Credential-free requests to [Hazaribag](https://shagun-peach.vercel.app/city/hazaribag) and all **12 distinct linked venue pages** returned **200**. The city heading and **12 venues** text were present. Every venue page contained **“Venue photograph not available”**, and no matching `/media/UUID/480`, `/960` or `/1600` reference appeared in its HTML. |
| Saved administration | The existing signed-in short-origin session read the city as **active** and all **12 venues as published**, with **0 drafts**. Every saved venue editor's photo section explicitly said **“No saved photos”**, with **0 private media-image elements**. This is missing image inventory, not evidence of broken delivery for an uploaded image. |
| Public login and autocomplete route | Peach `/admin/login` returned **200** with email/password in one form and no hosting-login redirect. `/api/cities/suggestions?q=haza` returned **200**; the earlier endpoint 404 is historical. These response checks do not constitute complete login/session or dropdown keyboard/browser acceptance. The short [admin login](https://shagun-admin.vercel.app/admin/login) remains available. |
| Provider observation | The existing [Production deployment page](https://vercel.com/richards-projects-224a1dea/shagun/9EoH6uVcDHH68wn6FBjgAAVEiimm) displayed **Ready**, **Production**, the peach domain and source **`6ecad8fcd55d37542dfec7284ec42c622715b125`**. This confirms the current provider state; no new deploy/promote operation was issued by this inspection. Local uncommitted backup tooling is not implied to be part of that source revision. |
| Photo inputs and rights | Workspace image-file search found no nonignored JPEG/PNG/WebP/AVIF/HEIC inputs in Shagun. The request for actual files or an approved folder/link, matching venue names and rights-holder permission returned automated unavailability rather than images or a licence. Prior gallery/open-licence searches remain bounded historical research; none cleared an image for use. No gallery image was copied, hotlinked or replaced with stock/generated photography. |

**Next photo action requires actual assets:** provide genuine images matched to the named venues, plus rights-holder permission or a verifiable reusable licence covering public hosting/resizing and required credit. Then use the existing authenticated image decode/re-encode, durable reservation and atomic-finalization path; preserve private permission references and public-safe credits. General listing approval does not authorise a third party's photos. Keep missing-photo labels truthful while these inputs are absent. The root-local SQL backup above predates the observed publication state and does not claim to contain future image bytes.

## Current local backup — 2026-09-10 UTC

**Completed as requested:** an encrypted backup is saved directly in the Shagun project root, with **Parimal** recorded as recovery owner. Its saved-file decryption and isolated PostgreSQL restore are verified. This is current Shagun SQL recovery evidence, **not an off-device copy or complete managed Auth/REST/Storage recovery**, and it does not promote the peach deployment or publish Hazaribag.

| Evidence | Verified result |
| --- | --- |
| Root archive | [Encrypted backup](../shagun-backup-2026-09-10T05-24-24-819Z-c21f69f6.enc), **79,411 bytes**; recovery owner **Parimal**. Root placement is explicitly owner-requested, not an inferred off-device destination. |
| Root receipt | [Verification manifest](../shagun-backup-2026-09-10T05-24-24-819Z-c21f69f6.manifest.json). Capture **2026-09-10T05:24:27.116Z**; successful existing-archive verification **2026-09-10T05:44:45.737Z**. Both files were verified ignored by Git. |
| Integrity | Encrypted-file SHA-256 **`8530a3d72021786ade265901f29bcb853d5f7f43e8dfc8b98719cf7041179987`**; native-archive SHA-256 **`851d27ca68643f626b813fbf3c9d357936946b03ae9adf3d96beda1e04e2a5ed`**. The already-created encrypted file, not a newly captured replacement, passed re-verification. |
| Current data | **1 city, 12 venues, 12 research rows, 19 facility associations, 9 facilities, 1 allowlist row, 3 limiter rows, 0 analytics/media/cleanup rows**, plus **5 private migration-ledger rows**. Exact row fingerprints matched the captured exported snapshot. These are capture-time counts, not proof of later live state. |
| Permissions/metadata | Restored owners, effective table/column/schema/function ACLs, RLS policies, constraints, indexes, user triggers, type/routine definitions and per-schema defaults matched the portable named snapshot. Two slug checks needed verified PostgreSQL parse/deparse normalization as described below; other fields remained exact comparisons. |
| Isolation/cleanup | Pinned PostgreSQL **17.6** image, new random container, **network none**, no exposed port, read-only root and tmpfs-only plaintext/database. The restore container was removed; a later label-filtered check found **0 remaining backup containers**. No hosted restore or sibling data mutation was executed. |
| Encryption | **AES-256-GCM** with **Windows DPAPI CurrentUser** key wrapping. Raw keys/credentials were not printed or written to files. Decryption requires the current Windows user profile; independent/portable key escrow is **not** established. |
| Local code verification | Final `npm run check` **PASS: 1,059 tests / 18 files**, lint and strict typecheck, on the uncommitted backup-tool changes. Added **56** portable snapshot, **5** encryption-envelope and **7** constraint-normalization cases. A final artifact/cleanup inspection and `git diff --check` passed. These are not new hosted CI, browser matrix or Production-build results. |

**Scope and exclusions:** native dump contains the current `shagun` and `shagun_private` schemas/data and ledger; encrypted bundle also contains migration sources and bounded Shagun Storage/reference metadata. It excludes Bihari customer/catalog rows, managed Auth user/password/session records, provider settings/secrets and global role passwords. The scratch restore uses UUID-only Auth reference stubs and the source `auth.uid()` SQL definition, **not functioning managed identities or validated sessions**. The bucket had no image objects; the runner refuses nonempty media/object/cleanup state rather than pretending to back up image bytes. The receipt explicitly records `offDeviceCopy: false`, `managedAuthRestored: false`, `managedStorageRestored: false` and `completeRecovery: false`.

**Resolved verification failure:** the first capture produced the encrypted file and restored all row fingerprints, but the metadata comparison failed for **`cities_slug_check`** and **`venues_slug_check`**. PostgreSQL transforms `BETWEEN` into a nested `AND`; native dump/reparse flattens that grouping without changing these predicates. An isolated PostgreSQL 17.6 reproduction confirmed the original/restored strings and fixed point. The final comparison reparses **both sides** on independent empty scratch tables, requires a fixed point and identical predicates, and preserves every other metadata comparison. Tests reject changed bounds/operators/regex/connectives and unrelated metadata changes. No migration/checksum, source constraint, archive or restored application table was rewritten to force a pass. The final run reopened the same encrypted file and wrote its first successful receipt.

**Implementation and safety:** [../scripts/db-backup-current.ts](../scripts/db-backup-current.ts), [../scripts/db-backup-snapshot.ts](../scripts/db-backup-snapshot.ts), [../scripts/backup-envelope.ts](../scripts/backup-envelope.ts), [../scripts/backup-key.ps1](../scripts/backup-key.ps1), and [../scripts/backup-constraint-normalization.ts](../scripts/backup-constraint-normalization.ts). The first strict typecheck found a missing explicit `NODE_ENV` in the subprocess environment; it was fixed and the final full check passed. The initial preflight subagent incorrectly described a Docker tag as immutable and the old OID-based metadata snapshot as portable; neither claim was used. Actual image ID/tool versions were inspected, and the new snapshot is tested across different role/type/function OIDs. The existing historical public-only backup helper was left unchanged.

## Earlier autocomplete and operator evidence — 2026-09-09

**At this earlier checkpoint, the owner's explicit editorial approval was saved for all 12 Hazaribag listings while they were private, unverified drafts.** The later observed publication/runtime state is recorded at the top, not inferred from these historical checks. The short [email/password admin login](https://shagun-admin.vercel.app/admin/login) was verified without Vercel login. Editorial approval need not be requested again for unchanged facts; full recovery and remaining managed-release acceptance are not supplied by that approval or by a subsequently reachable public URL.

| Current update label | Recorded result / next evidence |
| --- | --- |
| `AUTOCOMPLETE_RELEASE_RESULT` | **REVIEW PUBLICATION CONFIRMED / PRODUCTION BLOCKED:** application revision [c494a9c01c247b71ac5e5edeaaebfd0d326dfb80](https://github.com/RichardHenryJames/shagun/commit/c494a9c01c247b71ac5e5edeaaebfd0d326dfb80), followed by harness-only [ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f](https://github.com/RichardHenryJames/shagun/commit/ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f), are published on [audit/second-pass-2026-09-09](https://github.com/RichardHenryJames/shagun/tree/audit/second-pass-2026-09-09). The follow-up changes context-level interception for public delayed/error stubs, **not application Auth**. Publication is not pending and is not a Production release. |
| `AUTOCOMPLETE_HOSTED_CI_RESULT` | **PASS, both jobs:** [run 34350892886](https://github.com/RichardHenryJames/shagun/actions/runs/34350892886) completed successfully for `ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f`. GitHub separately confirms **Checks and isolated browser QA** and **Real local Auth, city publishing and Storage** both succeeded. Local totals below are not presented as fetched hosted totals. The failed parent run remains recorded below. |
| `LOCAL_INTEGRATION_STOP_RESULT` | **SUCCESS:** `npm run integration:stop` stopped the dedicated integration stack and retained its local data. The run's temporary random accounts had already been deleted; they are not available for handoff. This does not assert the state of unrelated containers. |
| `NORMAL_BUILD_RESTORATION_RESULT` | **PASS:** after the latest real integration run, the ordinary production build completed successfully with fixtures disabled. No migration, seed or provisioning was run by the build. |
| `NORMAL_PREVIEW_RESTORATION` | **PASS:** the normal preview task reported ready at localhost:3000. Fresh browser navigation found the city combobox and no synthetic banner; an anonymous `/api/cities/suggestions?q=haza` request returned **200**, `no-store`, and exactly an empty `items` array. This verifies the no-database preparation state, not a connected/public Hazaribag launch. |
| `CURRENT_DATA_API_VERIFICATION` | **RESTORED CONTRACT PASS:** after the guarded removal of 50 extra Shagun table grants, the complete read-only migration inspection passed all existing table/RPC/RLS/grant contracts, with all five checksums unchanged and no pending migrations. Fresh anonymous zero-row HTTP probes returned **401** for `admin_users` and `venue_research`, **200** for public cities, and **406** for unexposed `shagun_private`. The earlier zero-row private-table 200 responses were a real grant discrepancy, now corrected—not retrospectively a passing denial check. |
| `PRIVATE_OPERATOR_ACCESS` | **SUPABASE AND VERCEL ACCESS CONFIRMED:** earlier account/settings checks remain historical. In the short-domain operation, the Vercel owner UI accepted the scoped domain/configuration changes. At approximately **20:44 UTC**, the normal Supabase **Continue with GitHub** flow restored owner access and the actual scheduled-backups page loaded. No operator password, session cookie, API secret or token was copied or fabricated; no billing or managed Auth setting changed. |
| `PRODUCTION_ADMIN_PROVISIONING` | **AUTH ACCOUNT, ACTIVE ALLOWLIST AND PREVIEW SESSION CONFIRMED:** the owner created the approved account privately. The Auth UI and a bounded SQL check matched its actual UUID and approved email, with email confirmed and no active ban. At **13:30 UTC**, an insert-only exact-account operation added **one** `shagun.admin_users` entry; a separate SELECT verified it active. At **19:47 UTC**, after the owner's private login, the real administrator dashboard and city workspace were accessible. No duplicate identity, password reset or Auth-setting change was made. Passwords/hashes were not read; the local/helper minimum remains unchanged. This verifies Preview sign-in, not Production adoption or every session-lifecycle case. |
| `SHARED_OWNER_REMEDIATION_AUTHORIZATION` | **EXPLICITLY AUTHORIZED on 2026-09-09:** the owner directly approved the scoped BihariBhojan `Order` / `ContactMessage` security repair, preserving backend functionality and all data. This supersedes the earlier unavailable responses. Scope is limited to unintended API-role access to these two tables; no blanket schema, Auth, ownership, default-privilege or unrelated-data changes, and no automatic Production launch approval. |
| `SHARED_OWNER_REMEDIATION_RESULT` | **APPLIED AND VERIFIED:** the private committed receipt records removal of API-role/SQL PUBLIC privileges on only `Order` and `ContactMessage`. All four shared-table fingerprints and unrelated metadata across five schemas matched before/after; backend privileges were unchanged. Fresh read-only `anon` and `authenticated` role checks both deny both tables with SQLSTATE **42501**, and actual anonymous HTTP HEAD probes return **401**. The live Bihari categories/products/keepalive endpoints return **200**, and intentionally invalid order/contact submissions return **400** without creating records. |
| `CONFIGURED_ADMIN_PREVIEW` | **SHORT-ORIGIN REAL SESSION CONFIRMED:** **Preview** deployment `Ac46EgQcosryZRpimyonoMSNQqxm` redeploys unchanged source `6ecad8fcd55d37542dfec7284ec42c622715b125`, with branch-only `NEXT_PUBLIC_SITE_URL=https://shagun-admin.vercel.app`. The actual short-origin browser reached **Inventory overview** and the private city catalog returned **200/private/no-store** with its real session. The [Hazaribag workspace](https://shagun-admin.vercel.app/admin/cities/hazaribag) is still application-authenticated. No password/session was copied or fabricated; Production variables/domain and main were not promoted. |
| `SHORT_ADMIN_ACCESS_RESULT` | **PASS:** `shagun-admin.vercel.app` is a valid domain assigned only to **Preview / audit/second-pass-2026-09-09**. A persisted **single-domain Deployment Protection Exception** removes the extra Vercel login on that address; project-wide Standard Protection remains enabled. Fresh requests with no cookies/Authorization get **200** and the email/password form, **401** on private APIs, and **403** for a foreign-origin upload request. Both the old branch URL and old unique Preview still redirect to Vercel. No share-link or automation-bypass secret was created. |
| `PEACH_AUTOCOMPLETE_READINESS` | **NOT DEPLOYED — 20:49 UTC:** fresh credential-free requests to the customer origin found `/cities?q=haza` **200** with the no-matching-guides state, no Hazaribag guide link and no combobox role in its HTML; `/api/cities/suggestions?q=haza` returned **404**, as did `/city/hazaribag`. This is the old Production runtime, not a passing autocomplete check or just an empty suggestion response. Publishing a database record alone would not deploy the newer dropdown code. Production promotion remains gated independently of the completed short-domain admin setup. |
| `PRIVATE_HAZARIBAG_SEARCH` | **4 SAVED-HTML CHECKS PASS — 20:51 UTC:** the authenticated [city preview](https://shagun-admin.vercel.app/admin/cities/hazaribag/preview) returned the exact **12** candidate names with no query, **Tilak Baag only** for `q=tilak`, **Mehfil Banquet only** for `q=mehfil`, and **0** for a nonexistent venue query. All responses were **200/private/no-store** with no checked private follow-up marker. This is actual saved venue-search evidence, not public city-autocomplete, a hydrated visual/keyboard test or proof of publication. Fresh editor reads still found the city and all venues draft, with every review unchecked. |
| `MANAGED_BACKUP_READINESS` | **LOCAL CURRENT SQL BACKUP/RESTORE PASS; FULL RECOVERY OPEN:** the owner specified the **Shagun project root** and **Parimal**. The encrypted current archive and successful SQL-only restore are recorded in the newer section above, superseding the missing-destination/old-public-only status. Root-local storage is not off-device; Windows-profile key binding and excluded managed Auth/settings/object-service recovery remain explicit. No paid upgrade, hosted restore or release clearance occurred. Do not request the already supplied destination/custodian again. |
| `HAZARIBAG_MANAGED_IMPORT` | **12 DRAFTS CREATED, 0 SKIPPED:** the existing Hazaribag workspace's **Add researched drafts** action completed at approximately **19:48 UTC** through the signed-in administrator. Fresh reads of all 12 saved editors confirmed **draft / unverified / unreviewed**, blank check dates and nonempty private source notes. City status remains **draft**, published count **0**, media count **0**. The managed database inspection independently confirmed **12 venues and 12 research rows**; no duplicate city, photos, review, activation or public launch was created. |
| `HAZARIBAG_SOURCE_FOLLOWUP` | **SAVED, NOT EDITORIAL APPROVAL:** subsequent first-party/secondary rechecks were appended to all **12 private draft records** through their real, authenticated native server-action forms. Tilak Baag's description now states one advertised hall and the diesel exclusion; Aranya's unqualified AC/backup flags were withheld; Mehfil's public description qualifies its advertised 275-guest maximum. The five primary-evidence holds remain unresolved. No review checkbox, verified date, publication status or city lifecycle was advanced. |
| `HAZARIBAG_EDITORIAL_APPROVAL` | **OWNER APPROVED / 12 SAVED AND RECHECKED:** the owner explicitly stated they had reviewed all saved Hazaribag listings, confirmed the information correct and asked to proceed. At **20:58 UTC**, the existing real administrator session saved the review confirmation and a private approval note on each of the **12 existing drafts**, using exact optimistic versions and the native atomic save action. All factual fields/facilities were compared before/after and preserved. At **20:59 UTC**, separate fresh reads confirmed **12 reviewed drafts**, unchanged unverified status and blank check dates. City remains draft; no publication, invented direct verification or photo permission was recorded. |
| `HAZARIBAG_PHOTO_RIGHTS` | **BLOCKED:** no approved image files or asset-specific permission were supplied. The seven website-backed candidates' inspected gallery/footer/legal pages provided no usable photo-republication grant; the remaining primary Facebook pages required login. Bounded exact-venue searches of Wikimedia Commons/Openverse established no suitable image/author/licence match. This is not proof that images do not exist elsewhere. **0 venue images copied into the project, 0 media uploads**, and no stock/generated/other-city substitute. |

**Historical public URL/access observation after the short-domain operation:** the peach city page then returned **404** and its login showed configuration unavailable. Those responses are superseded by the **07:20–07:22 UTC, 10 September** observations at the top. The short [admin login](https://shagun-admin.vercel.app/admin/login) remains an ordinary email/password entry point; sessions are host-scoped and were not copied between domains. No further hosting-protection change was made in the image inspection.

**Earlier review deployment:** GitHub recorded deployment **6349647523** for `ce533444` as **Preview**, `production_environment=false`, state **success**, at [https://shagun-c9t7iwgbu-richards-projects-224a1dea.vercel.app](https://shagun-c9t7iwgbu-richards-projects-224a1dea.vercel.app). At that observation remote `main` was **`b62d6eda5fde1fe5e588ef3a4b2a5cdeb4693908`** and the review branch was **`ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f`**. These are historical branch observations; the later configured Preview is recorded in `CONFIGURED_ADMIN_PREVIEW`, not inferred from this deployment.

**Historical live deployment mismatch at 13:37–13:38 UTC:** the latest GitHub Production deployment record then was **6327414164**, created **2026-09-08T12:19:34Z**, at **`b62d6eda5fde1fe5e588ef3a4b2a5cdeb4693908`**. The later autocomplete/recovery changes were on the review branch, not this deployed revision. Saved variables and a newly allowlisted account did not rebuild that deployment. No Production promotion has been performed in the subsequent repair/Preview work. The earlier unavailable approval response was later superseded by the owner's explicit authorization, recorded above; do not keep asking for the same scoped repair approval.

**Release-safety work still open:** the short login, real sign-in, import, source corrections, owner editorial approval, scoped permission repairs and local SQL backup are recorded as completed. The later observed public state does not certify full managed recovery or remaining release acceptance; do not bypass those requirements for further deployment changes. Do not recreate the account, re-import, repeat approval for unchanged facts or request passwords. Owner review does not erase private source uncertainties, prove business contacts or authorise image reuse. **Missing photos alone does not prohibit an otherwise approved text-only listing**, but the image-completion request remains blocked on actual authorised photos. Future factual edits must follow the normal review-invalidating/versioned workflow.

**Backup handoff update:** the subsequent explicit destination is the **local Shagun project root**, with **Parimal** responsible for recovery. That requested local backup is now completed and verified as detailed above. Off-device retention, independent key custody and supported managed-service recovery remain additional work, not missing approval for the completed local artifact. No encryption credentials should be supplied through chat.

### Explicit owner review recorded — 20:58–20:59 UTC

- **Approval provenance:** the owner directly confirmed that they reviewed the Hazaribag listings and found the information correct. This is a real supplied editorial decision, not an inference from an import, website response or generic request to bypass checks. A dated private note records the limited scope of that approval while preserving every prior source/caveat paragraph. No new business fact, phone call, message, venue visit or photo-rights grant was asserted.
- **Authenticated atomic saves:** all 12 existing IDs/names/city associations were checked against the saved Hazaribag inventory. Each fresh native form retained its bound action and exact `expected_updated_at`; the review checkbox and appended private approval note were the only requested changes. The real `save_venue` RPC supplies the transaction-time review stamp and current managed UUID; no reviewer/date was fabricated in the request. All factual fields, facility selections, status, URLs, city association, SEO values and blank check dates matched before/after. No direct SQL/service-key inventory update or duplicate import was used.
- **Fresh persistence and privacy:** independent editor reads confirmed the review checkbox and private approval note on **all 12 drafts**, still unverified with blank check dates. The city remained draft; public city returned **404** and public suggestions remained empty. The checked approval-note marker was absent from the saved public-layout city preview. These are actual review saves and reads, not a publication or full managed-acceptance run. The dashboard's **review/recheck** count may remain nonzero because unverified records still need information checks; do not mark them verified merely to clear that counter.
- **Remaining recovery dependency:** the dedicated backup folder contained only the two previously recorded historical runs. The later manifest explicitly covers only the public schema, with Auth/Storage counts zero at that old checkpoint and no original owner/ACL replay. It does not cover the now-reviewed Shagun records or managed identity/settings recovery. No approved off-device destination or equivalent existing recovery evidence was supplied in the subsequent answer. No backup guard was weakened, no shared data exported to an unapproved destination, and no billing change or Production promotion occurred.

### Short email/password admin, scoped hosting exception — 2026-09-09

- **Explicit user request and scope:** the owner requested a shorter URL and no extra Vercel login. Added **`shagun-admin.vercel.app`** through the existing project's domain UI, explicitly selected **Preview** and **audit/second-pass-2026-09-09**, and verified **Valid Configuration**. Existing `shagun-peach.vercel.app` stayed assigned to Production. No new paid project/domain purchase, production-branch change or main promotion occurred.
- **Exact-origin configuration:** edited only the existing review-branch `NEXT_PUBLIC_SITE_URL` from the generated long branch origin to `https://shagun-admin.vercel.app`. Production values and all server secrets were untouched. Redeployed the same source with **Preview selected and build-cache reuse off**; new [deployment](https://vercel.com/richards-projects-224a1dea/shagun/Ac46EgQcosryZRpimyonoMSNQqxm) was **Ready** at [its unique URL](https://shagun-gkkxdgtou-richards-projects-224a1dea.vercel.app), with the short domain assigned. The built application is unchanged; this is provider-configuration/rebuild evidence, not a new application-test run. Use the short origin for subsequent admin forms/uploads rather than the obsolete configured origin.
- **Hosting protection, not application authorization:** confirmed the exact short hostname and the provider's required confirmation text, then saved a **one-domain exception**. The persisted exceptions table contains that one hostname and **Require Log In / Standard Protection remains enabled** for other deployment URLs. No global disable, wildcard, secret bypass link, forged Vercel host flag, Auth bypass, RLS change, managed account change or source-map-protection edit was made. Every private Shagun route/action/preview still requires managed Auth and active UUID membership.
- **Independent no-credential checks — all 10 passed:** short login **200**, real email/password fields and no Vercel redirect; private city catalog **401**; robots **200/disallow all**; sitemap **404**; old branch and old unique Preview login addresses **302 to vercel.com/sso-api**; Production login remains unconfigured **200**; Production Hazaribag **404**; empty anonymous upload with the exact short `Origin` **401**; the same empty request with `Origin: https://example.com` **403**. These were bounded guard checks, not actual uploads or artificial hosted inventory. No Cookie/Authorization values were sent, copied or printed.
- **Browser/private-route checks:** a fresh short-domain browser rendered **Sign in to Shagun** directly. Later that origin showed the real **Inventory overview** and its private catalog returned **200/private/no-store**, confirming an actual permitted session without reading passwords. Separate credential-omitted reads of `/admin` and both city/venue saved previews contained only the native redirect-to-login instruction, with no checked private venue/source content. Those protected HTML responses were streamed **200 redirects**, not claimed as HTTP 401/307; private APIs returned 401. Do not treat the anonymous denial probes as another sign-in or a full expiry/revocation test.
- **Fresh saved editorial state — 20:42 UTC:** authenticated reads on the short origin found the existing city **draft** and all **12 venues draft/unverified/unreviewed**. No venue status, editorial-review flag, source fact, city lifecycle or photo was changed during this login operation. Existing source corrections remain saved; no repeat import was run.
- **Recovery inspection:** a stale/incomplete dashboard page was not treated as current backup evidence. The normal Supabase GitHub owner flow restored access; the actual scheduled-backups page then confirmed the **Free plan has no project backups**. No production restore or subscription change was attempted, and the existing public-only backup helper was not run or weakened to claim current full recovery. A manual protected/off-device backup/rehearsal path needs an approved destination/owner; a paid provider option needs explicit spending approval. Neither option is selected by an automated unavailable response.
- **Search-probe scope:** the first raw-HTML check assumed every card heading remained an `article` descendant. Streamed HTML can place a heading in a later hidden segment before browser hydration, so that assumption missed a saved result. Inspection found all 12 headings and preview links; the corrected four-case probe checked exact names and distinct preview links independently and passed. No application selector, production code, visibility predicate or assertion on the required result sets was weakened. The peach-domain missing autocomplete endpoint is a separate, freshly observed deployment gap.

### Web-source and photo-rights follow-up — 2026-09-09

- **Scope:** rechecked first-party source pages for the seven website-backed candidates and revisited the supplied primary/secondary links for the five holds. No telephone/WhatsApp enquiry, venue visit or business-operation confirmation was made. Source-page accessibility is not verification or an exhaustive Hazaribag inventory. The original dated JSON was left intact as the initial import artefact; existing draft corrections were saved separately rather than overwriting records through a repeat import.
- **Conservative saved corrections:** [Tilak Baag facilities](https://www.tilakbaag.com/facilities.html) describes another small hall as in process and a DG set without diesel. The description now refers to one advertised AC hall and explicitly qualifies the generator; the unqualified backup badge was removed. [Aranya's homepage](https://hotelaranyavihar.com/) and [Victoria](https://hotelaranyavihar.com/victoria/) do not establish specific event-hall cooling/backup arrangements; those unqualified badges were removed, with rooms/catering retained and no claim of actual unavailability. The displayed/link email discrepancy remains, so public email stays blank. [Mehfil](https://mehfilbanquet.com/)'s 275-guest figure is now explicitly qualified as advertised and layout-dependent in the description; event prices remain blank.
- **Other saved follow-ups:** Floresta's 1,000/700 capacity and hall-label scope remain unresolved; no numeric capacity was added. [Ananta's terms](https://anantabythehill.com/terms-and-conditions) supply a first-party Hirabaug/825301 address variant despite the contact page omitting a PIN; the variant was recorded privately, not silently merged into the saved address. Sunshine's station/entrance ambiguity and [Rana Palace text](https://www.hotelsunshineheritage.in/our-services/weddings-receptions/) remain; those claims are excluded. Hill Side still has locality-level address evidence and generic gallery captions, not authenticated image provenance.
- **Five holds retained:** the supplied Facebook business/photo pages required login; no restriction was bypassed. Readable Mandap/EventPlanet corroboration does not resolve primary contacts or entrances. Dikshit's Grand has a secondary Chisti Nagar variant versus the recorded primary Ravindra Path wording; Rings & Roses retains conflicting room/parking/capacity figures and no confirmed direct contact; the Taj directory still contains an Indore mismatch. No directory lead-routing number, additional PIN or unsupported facility was adopted.
- **Licence search:** inspected galleries, copyright/footer notices and linked terms/privacy pages where available. Ordinary gallery access and rights-reserved notices provide no image-republication permission. Bounded Wikimedia Commons/Openverse searches covered exact/distinctive names, spelling variants and Hazaribag/Hazaribagh/Jharkhand qualifiers, with capped results rather than exhaustive pagination. An open-licence [Taj Banquet Centre near-match](https://commons.wikimedia.org/w/index.php?curid=29298900) depicts **Toronto**, not Hazaribag, and was rejected. No acceptable photo was found or copied to local assets/Storage. The required rights-holder handoff is in [HAZARIBAG_RESEARCH.md](HAZARIBAG_RESEARCH.md#photo-handoff-needed-from-the-rights-holder).
- **Mutation boundary:** all 12 source-note updates and three description corrections used fresh rendered multipart forms and the owner's real session, preserving each exact `expected_updated_at` and the existing bound server action. Status/review guards refused changes to anything other than the expected draft/unverified/unreviewed state; facilities and descriptions were checked before targeted replacement. No SQL/service-role inventory write, impersonation or bypass was used. These were authenticated native form submissions, **not a newly passing browser click/visibility workflow**: the background integrated editor stayed in a hidden streamed segment despite loaded scripts. No DOM visibility, Auth, preview protection or application code was altered to force a pass.
- **Read-only database postcheck:** existing migration/grant/RLS contracts passed with **0 pending migrations**, **12 venues / 12 research rows / 19 facility associations**, **0 media / 0 storage objects / 0 cleanup jobs**, and the existing **1 city / 1 Auth account / 1 allowlist entry**. The three deliberately withheld facility associations account for the change from the import's 22. Shared Category/Product/Order/ContactMessage counts remained **6/44/0/0** with private API SELECT denied. `preservationComparisonPerformed: false` remains a read-only inspection boundary, not a rerun of historical apply-time comparisons.
- **Independent HTTP persistence/privacy recheck — 20:17 UTC:** fresh reads of every saved editor confirmed the dated follow-up, draft/unverified state, unchecked review, blank check date and the three targeted descriptions/facility changes. All **12 public detail URLs returned 404**, city status remained draft and public suggestions remained empty despite the real administrator cookie. The saved city preview omitted the checked private follow-up marker. The private catalog returned **200/private/no-store** and the configured Preview login returned **200 with the real sign-in form**. The login page intentionally renders that form even for an existing session: an earlier probe wrongly expected a redirect and was corrected after reading its actual implementation; no account/session reset or application change was needed. This is not a full managed session-lifecycle, visual or final-domain acceptance pass.

### Real administrator session and Hazaribag draft import — 19:47–19:52 UTC

- **Real UI execution:** after the owner reported signing in privately, the shared browser showed the protected administrator inventory, then **Cities → Hazaribag** with zero saved venues. One explicit **Add researched drafts** submission returned **“Created 12 drafts; skipped 0 existing venues”**. The existing city was reused; no service-key session, Auth mock, SQL impersonation or direct database insert replaced the application action.
- **Persistence:** the saved city preview rendered **12 real candidate cards**, all labelled draft, with honest missing-photo illustrations. Separate authenticated HTTP reads of every saved venue editor confirmed draft status, unverified state, unchecked editorial-review confirmation, empty check date and private source notes. This checks saved import state, not the accuracy of all venue claims. No fact corrections, source rechecks, completed reviews or contact confirmations are inferred from it.
- **Managed access and separation:** with the owner's real Shagun session and legitimate Vercel access retained, the private city catalog and saved city/venue previews returned **200**, private/no-store and noindex. Public city suggestions still returned exactly an empty `items` array; public Hazaribag and the Tilak Baag public detail path returned actual **404** even with the admin cookie present. Checked private-source markers were absent from saved city/venue preview HTML; this bounded check is not a universal leak audit. Credential-free customer-domain results are recorded above. No credentials were read, exported or supplied by the assistant.
- **Fresh read-only database inspection:** passed the existing contracts for **10 tables / 19 application RPCs**, all **five unchanged migration checksums**, and **0 pending migrations**. Counts: **1 city, 12 venues, 12 private research rows, 22 venue-facility associations, 0 media assets, 0 storage objects, 0 cleanup jobs, 1 Auth account and 1 allowlist entry**. Shared Category/Product/Order/ContactMessage counts remained **6/44/0/0**, with private-table API-role SELECT still denied. The inspection reported `preservationComparisonPerformed: false`; it neither applied a migration/seed nor repeated historical transaction-level preservation comparisons.
- **Scope still open:** this is actual managed login, authenticated draft creation and saved-read evidence, not managed logout/expiry/revocation, concurrent-editor/fault recovery, authorized image upload, final-domain acceptance or public publication. No main/Production deployment, city activation or new synthetic hosted inventory occurred. The owner session remains available; do not sign it out merely to rerun already-passed local workflows.

### Authorized permission repairs and configured Preview — 2026-09-09

- **Bihari repair:** the committed private receipt under ignored local security evidence records only the two approved table-ACL changes. The configured Prisma connection's principal was verified as `postgres`. Before and after, read-only zero-row queries and **EXPLAIN without ANALYZE** checked insert/returning permissions without executing inserts. An isolated PGlite rehearsal tested real inserts/returned rows, API-role denial, preserved existing rows/catalog access and rollback. The five live HTTP smoke checks were public reads/invalid submissions, not successful production orders or messages. These facts do not certify unexamined external consumers or full service recovery.
- **Shagun reconciliation:** read-only inspection found 50 extra table grants beyond the installed contract, with no function-grant differences. The first transaction **rolled back** on a preservation assertion because PostgreSQL table-level `REVOKE UPDATE` also clears the existing cleanup `UPDATE(id)` grant. A local isolated PostgreSQL check reproduced that behavior. The corrected operation restored only that preexisting column grant, removed the 50 extra table grants and committed after exact metadata/fingerprint comparisons. All data, policies, functions, owners, the active administrator and all sibling metadata were preserved. A subsequent standard read-only inspection passed and reported **1 Auth account, 1 active allowlist record, 1 city, 0 venues, 0 media**, with **0 pending migrations**. This read-only verification did not rerun the historical installation's seven apply-time checks.
- **Preview credentials:** one new Supabase server key, named `shagun_admin_review_20260909`, was created and stored as a Vercel **Secret** only for `audit/second-pass-2026-09-09`; a separate random limiter Secret was generated for the same branch. The new key was transiently revealed in its legitimate dashboard UI for direct in-memory transfer to Vercel and hidden afterward; its value was not returned in the handoff or written to local source. Existing Production secrets were not retrieved or replaced. This is a separate credential, **not a separate Supabase project or a reduction in service-key privilege**. It must remain server-only and requires revocation when no longer used. This supersedes earlier statements that no backend key had ever been revealed during the entire session.
- **Preview configuration:** the branch alone has the approved Supabase URL/publishable key, its exact Vercel branch origin, fixtures=false and analytics=false. The redeploy dialog was verified **Preview**, with the same reviewed branch source and build-cache reuse off. Both backend/limiter values are Secret; no values were saved to Production, other Preview branches, source control or a plaintext local environment file. Deployment completed **Ready** and the actual page rendered the credential form.
- **Earlier pre-login Preview smoke:** with legitimate Vercel Preview-access cookies but **no Shagun login**, suggestions returned **200** with an empty `items` array; the private city catalog returned **401/private/no-store**; draft Hazaribag returned **404**; robots disallowed all crawling. That earlier smoke did not verify administrator login; the later real-session checks are separate above. A credential-free request is intercepted by Vercel's access gate, not a Shagun customer gate.

### Managed administrator verification — 2026-09-09

The private operator check selected only the approved Auth account's UUID/email, confirmation/ban flags and matching allowlist state; neither its password nor password hash was selected. The one-row allowlist INSERT selected that verified Auth UUID, required the exact approved email and confirmation/no-ban conditions, and used `ON CONFLICT (id) DO NOTHING`, never reactivating or overwriting an existing membership. The subsequent read returned the same UUID with `is_active=true` and the display label “Shagun administrator”. No research, city, venue, media or sibling table was modified.

At the administrator-verification checkpoint, the API probes used only the existing **publishable** key from its isolated dashboard section. That public value was visible in the dashboard/tool snapshot; no backend secret was used for those probes and no user session was substituted for anonymous access. RLS metadata showed `admin_self_read` for authenticated self-read and `research_admin` for authenticated active admins only. The additional anonymous SELECT grants were a managed configuration discrepancy, not successful grant-level denials; their later correction is recorded above. No keys or passwords were written into source.

### Published application CI failure and harness-only follow-up

[Run 34337558019](https://github.com/RichardHenryJames/shagun/actions/runs/34337558019) at **`c494a9c01c247b71ac5e5edeaaebfd0d326dfb80` FAILED**: the full `many` scenario recorded **185 passed / 1 skipped / 1 failed**, with `route.continue` attempting to handle an already handled route. This was one public browser harness failure, **not all tests failing**. That failed run has **no passing real-Auth job**; earlier hosted Auth passes do not supply one for it.

The published `ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f` follow-up moves the public delayed/error stubs to the **browser-context interception layer**, retaining owned fulfillment, awaited gates, cleanup and assertions. It is a **harness-only repair**, not an application authorization change or a mock of the real integration Auth workflow.

### Local check and public browser matrix

- **Current full check PASS:** lint, route type generation, strict typecheck and **991 tests across 15 files**, including **39 city-suggestion unit tests**, with **no warnings**, on the working code published as `ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f`.
- **Current focused harness repeat PASS:** **20 repeat cases**—two tests × two widths × five repeats—in **1.7 minutes**, at the context-layer working fix. These focused cases are not added to full-matrix totals.
- **Current fresh full `many` PASS:** **186 passed / 1 intentional skip in 2.8 minutes**, at **320, 375, 390, 414, 768 and 1440 px**, on the same working fix. **`empty` and `one` were not rerun after this context-layer-only repair.**

**Earlier autocomplete matrix — preserved history, before the context-layer-only fix:** the following runs followed the `MediaPhoto` fallback fix, at all six widths in every scenario. The earlier callback-ownership repair preceded the recorded `empty` and `one` runs.

| Scenario | Passed | Intentional skips | Duration |
| --- | ---: | ---: | --- |
| `many` | 186 | 1 | 2.5 minutes |
| `empty` | 109 | 34 | 2.9 minutes |
| `one` | 140 | 21 | 2.4 minutes |
| **Total** | **435** | **56** | All three scenarios, all six widths |

The prior **435 passes / 56 intentional, inapplicable skips** remain valid **only at that earlier scope**, not as a newly rerun full matrix at `ce533444`. They are not 491 passes. Coverage adds **seven autocomplete browser tests** and a deterministic image pre-hydration test applicable only to nonempty inventory. No assertions were weakened; neither focused repeats nor the fresh `many` rerun are added to this historical total.

- **Image regression:** the first full `many` run exposed image-fallback failures. `MediaPhoto` now checks `node.complete` and `naturalWidth` without requiring `currentSrc`. A deterministic regression fails an SSR image while hydration is held; a healthy-image control and the focused gallery target passed **6/6**. This closes a conditional detection gap; the old failure did **not** capture runtime evidence proving `currentSrc` was empty.
- **Earlier harness regression:** the initial `empty` run failed when the delayed-response helper attempted to continue an already handled route. The callback-ownership repair and exact **six repeat phone/desktop cases passed** before the recorded `empty` and `one` runs. That evidence remains historical; it did not prevent the later hosted recurrence, which led to the context-layer-only repair above. Neither repair waived an application assertion.

### Current real local integration — post-sitemap PASS

On the same code as **`ce533444a26e50cd42c4be2b0d4cb9f2a0703d5f`**, `npm run build:integration` **passed**, then `npm run test:integration` passed **3/3 workflows in 2.7 minutes**, at **390, 768 and 1440 px**, with **all 12 substeps each**. This is current **post-sitemap** owned-service evidence, not the older pre-refactor run. These are three complete workflows, not 36 independently counted tests.

- Real random managed **local** email/password accounts exercise wrong-password rejection, actual login/refresh and ordinary non-admin denial. No Auth bypass or fabricated session is used.
- The UI creates a synthetic Chapra test city and venue. **New city-cover coverage** uploads actual PNG bytes, verifies responsive variants and authenticated private preview, and keeps the cover hidden publicly until city activation. Venue coverage uploads actual PNG/JPEG/WebP bytes and exercises cover selection, reordering, editing and deletion.
- Publication/activation verifies public autocomplete alongside valid public inventory counts; suggestions remain a name/slug/state projection. An admin cookie never widens public visibility to drafts. An active guide remains discoverable when empty until explicitly deactivated.
- Cleanup removes **only exact run-owned records** and all city-cover/venue variants, preserving Hazaribag. Every random local account created for the run was removed. No persistent production administrator or production test inventory was created.

This uses actual isolated Auth/REST/Storage/PostgreSQL at API **127.0.0.1:55321**, database **55322**, and app **localhost:3200**, with **`SHAGUN_TEST_FIXTURES=false`**. Inventory/images are synthetic and local; this is **not managed Production acceptance**. Test-owned record/media/account cleanup completed. Stack shutdown, the normal production rebuild and fresh localhost:3000 verification succeeded as recorded above. **No test accounts remain available for handoff.**

**Earlier autocomplete checkpoint:** the previously recorded local integration run passed **3/3 in 2.3 minutes**, with 12 substeps at the same three widths. Its stack stop, normal fixtures-false build and localhost:3000 restoration also passed at that checkpoint; only unrelated `biharibhojan-db` remained running then. The local `haza` combobox showed **“No public city guides match yet.”**, without synthetic inventory, and the hosted admin login was configuration-unavailable at that observation. These historical results are not the latest 2.7-minute run, current container/preview state or fresh hosted-admin verification.

### Earlier shared read-only inspection — PASS, not another apply

At that inspection, the read-only results were **1 city, 0 venues, 0 media assets, 0 administrators and 0 managed Auth users**. All **five ledger checksums were unchanged**, with **0 pending migrations**; sibling data matched the recorded baseline. This operation reported **`preservationComparisonPerformed: false`**: do not present the seven historical apply-time preservation flags below as a fresh seven-check comparison. **No production SQL mutations were performed in this inspection.** These SQL counts do not establish fresh Data API or public readiness; use the current operator labels above.

## Historical catalog/review results and labels

The following labels describe the **earlier catalog revisions**, not the current autocomplete work. `FINAL_RELEASE_RESULT` distinguished successful review publication, hosted checks for both catalog-era application revisions and blocked production. Their completed runs remain historical evidence; consult the current labels above for the latest work.

| Label | Recorded result / next evidence |
| --- | --- |
| `FINAL_CHECK_RESULT` | **PASS after `9db51bd`:** `npm run check` passed lint, route type generation, strict typecheck and **952 tests across 14 files**. The **60 catalog tests** include two new missing-URL/missing-public-key cases; the **19 request-time sitemap tests** and integration-reset guards remain included. |
| `LOCAL_INTEGRATION_BUILD` | **PASS:** `npm run build:integration`, a production-mode Next build for the exact local Supabase/localhost:3200 environment, fixtures disabled, before the sitemap refactor. Not a normal release build or deployment. |
| `LOCAL_INTEGRATION_BROWSER` | **PASS:** `npm run test:integration` completed **3/3 workflows on Windows**, at **390, 768 and 1440 px**, **11 substeps each**, in **2.4 minutes**, after the `.a-table-wrap` positioning fix and **before the sitemap refactor**. Real local Auth, REST, PostgreSQL and Storage plus signed-in axe/no-overflow checks were used. This historical run and the separate hosted `2072dc7` run have different scopes from the new 12-substep run above. |
| `FINAL_NORMAL_BUILD_RESULT` | **PASS after `9db51bd`:** normal `npm run build` with **`SHAGUN_TEST_FIXTURES=false`**. Request-time sitemaps are dynamic, with **no database queries during compilation**; builds never migrate, seed or provision users. The normal localhost:3000 empty preview was restored and confirmed at that checkpoint, not the current one. |
| `FINAL_FIXTURE_MATRIX_RESULT` | **PASS, Windows matrix before the final catalog-only guard:** `many` **166/1**, `empty` **95/28**, and `one` **120/21**, all at **six widths** (passed/skipped). **381 passes / 50 intentional, inapplicable skips**, no failures. Hosted audit CI ran only `many`, not this whole matrix. |
| `FOLLOW_UP_HOSTED_CI_RESULT` | **PASS, both jobs:** [run 34322083604](https://github.com/RichardHenryJames/shagun/actions/runs/34322083604) completed successfully at **`9db51bd5b0a11395d62401353fde3e0db03a59fe`**. Actual logs confirm Node 22 full check **952 tests / 14 files**, `many` public fixtures **166 passed / 1 skipped**, and Node 24 real isolated Auth/Storage workflows **3 passed in 1.8 minutes**, followed by successful stack shutdown. |
| `FINAL_RELEASE_RESULT` | **HISTORICAL REVIEW PUBLICATION: SUCCESS / CATALOG APPLICATION CI: PASS / PRODUCTION: BLOCKED.** Published [audit/second-pass-2026-09-09](https://github.com/RichardHenryJames/shagun/tree/audit/second-pass-2026-09-09) contains the tested follow-up [9db51bd](https://github.com/RichardHenryJames/shagun/commit/9db51bd5b0a11395d62401353fde3e0db03a59fe), not just original `2072dc7`. Its own hosted CI and Preview smoke passed within their stated scopes. Only Preview deployment was recorded; remote/local `main` were unchanged at that observation. Critical shared-security, API, real-admin, reviewed-inventory and full managed-acceptance/recovery gates remain open. |

Toolchain: Windows, locally tested Node **24**; pinned Next.js **16.3.3**, TypeScript **5.9.3** and development-only Supabase CLI **2.116.0**. Reproduce with [../package.json](../package.json) and [../package-lock.json](../package-lock.json).

## Managed SQL installation — historical apply PASS

The real shared Supabase target was **`ixkhyqqovacdramymqjk`**. After the empty-ACL catalog fix and pre-apply backup rehearsal, the guarded runner completed **`--apply --seed` successfully**. All five migrations **0001–0005**, including the city-preview RPCs, were applied and recorded in the private checksum ledger. TLS certificate/hostname verification remained enabled; existing managed Storage-policy delegation was used without ownership escalation.

| Observed postcondition | Result |
| --- | --- |
| Shagun objects | **10 tables, 19 RPCs in schema `shagun`**; private ledger excluded from the table count; RLS and role permissions verified. “19 RPCs” does **not** mean 19 anonymously executable functions. |
| Inventory | `admin_users=0`, `cities=1` (draft Hazaribag), `facilities=9`, `venues=0`, `media_assets=0`, `storage_cleanup_jobs=0`. |
| Storage | Private **`shagun-media`** bucket verified; **6 Shagun policies** verified. |
| Shared baseline | BihariBhojan **Category 6, Product 44, Order 0, ContactMessage 0**, with unchanged row fingerprints. Managed **Auth user count 0**. |

All **seven** preservation flags were **true**: `publicMetadataUnchanged`, `authMetadataUnchanged`, `storageMetadataUnchanged`, `storageBucketsUnchanged`, `sharedDataUnchanged`, `authCountUnchanged`, `storageObjectCountUnchanged`.

The comparisons exclude only the intended new bucket/six policies and the internal Auth FK support triggers belonging to the new Shagun allowlist constraint. Existing sibling data, managed definitions, ownership, base grants and unrelated policies were preserved. The runner did not change hosted API exposure or shared Auth settings. Repeatable-read fingerprints check this operation's baseline/changes; they do not audit unrelated concurrent transactions or prove the preexisting configuration secure.

Sources: [../scripts/db-migrate.ts](../scripts/db-migrate.ts), [../scripts/db-migration-plan.ts](../scripts/db-migration-plan.ts), [../scripts/db-catalog-snapshot.ts](../scripts/db-catalog-snapshot.ts). The former metadata `22023` failure is **resolved**, not a current installation blocker.

## Pre-apply backup and local restore — LIMITED PASS

The pre-install **public-schema backup plus Auth/Storage logical metadata** is retained privately under [../.qa/backups/](../.qa/backups/), excluded by [../.gitignore](../.gitignore). [../scripts/db-backup.ts](../scripts/db-backup.ts) restored the archive into a **new local scratch database**, verified the shared-table fingerprints and archive integrity, and completed scratch/copy cleanup. No restore was applied to the managed project.

The archive/metadata retain ownership and ACL information, but the scratch restore used **`--no-owner --no-privileges`**. Original ownership/ACLs were **not replayed**, and role restoration was **not tested**. This was not a managed Auth identity/settings, object-byte or full service recovery rehearsal. The artifact predates Shagun installation and is not a current Shagun inventory backup. Local private files are not automatically encrypted/off-site; complete recovery remains open.

## Production configuration and inventory — NOT LAUNCHED

- Vercel **Production** already has `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` as **Config** and `SUPABASE_SERVICE_ROLE_KEY` as **Secret**, plus project/site URLs, fixtures=false, analytics=false and a generated `RATE_LIMIT_SECRET`. Do not ask for the keys again. Their storage does not establish backend-key validity, runtime adoption or a working admin session.
- **The approved account, active UUID membership and real managed Preview session are verified**, as recorded above. The owner set and entered the password privately; no password/hash was inspected or reset here. Production runtime adoption is not established by that separate Preview session. Local Auth and the trusted provisioning helper retain their **12-character minimum** (the helper accepts **12–128**); no chat-supplied credential was persisted and no password policy was weakened.
- The **12 real source-cited Hazaribag candidates are private drafts with explicit owner editorial approval now saved**, still unverified with no photos or established rights. The owner's review is distinct from the earlier import/source checks; it does not claim verified contacts or exhaustive coverage.
- The later web-source follow-up is recorded above, separately from the original seven-source HTTP check. The Aranya email mismatch remains unresolved, and its unqualified hall AC/backup claims were withheld rather than falsely confirmed. Five candidates retain primary/contact holds. These source rechecks do not establish contact verification, photo rights, completed editorial approval or public readiness. See [HAZARIBAG_RESEARCH.md](HAZARIBAG_RESEARCH.md).
- **No Chapra production record has been created.** Integration workflows use explicitly synthetic, temporary local records. The last recorded public `/cities?q=haza` search showed no matching guides; it does not establish fresh API readiness or launch acceptance.

## Historical managed HTTP probes — confirmed shared exposure and earlier schema failure

These probes preserve the earlier evidence, not a fresh current HTTP check. See `CURRENT_DATA_API_VERIFICATION` and the later managed-session/import evidence above for restored grants and current successful application access; **do not present the old 406 as the current failure**.

The public publishable key was **momentarily revealed in the browser UI**, used only in browser memory for legitimate read-only, zero-row probes, then hidden before tool results. Its value was not printed or returned to the model. **Private/backend secrets were not read or revealed**; this is not a claim that no key was ever shown in the UI.

| Historical anonymous probe | Observed result at that checkpoint |
| --- | --- |
| `HEAD /rest/v1/Order?select=id&limit=0` | **200**; existing shared `public.Order` API access confirmed. |
| `HEAD /rest/v1/ContactMessage?select=id&limit=0` | **200**; existing shared `public.ContactMessage` API access confirmed. |
| `/rest/v1/cities?select=id&limit=0` with `Accept-Profile: shagun` | **406 / PGRST106 / Invalid schema: shagun**. |

**No customer rows were retrieved.** The first two results corroborate the already observed anonymous/authenticated SELECT grants with RLS disabled. Those probes established reachability, not merely hypothetical risk; the empty Order/ContactMessage baseline does not protect future rows. This preexisting security risk was **not changed by Shagun installation**. Remediation requires **separate shared/BihariBhojan-owner authorization**, never unilateral sibling grants/RLS changes or customer-row retrieval. Historical preservation success is not security clearance.

An authorized operator must verify the actual exposed-schema list and append `shagun` **only if absent**, preserving **every** other entry and excluding `shagun_private`. **At this earlier checkpoint**, the expired dashboard session's management request returned **401** and the GitHub button did not navigate. The later successful navigation to the real GitHub login is recorded in `PRIVATE_OPERATOR_ACCESS`; the old 401 is not a fresh status for that redirect. No API-list or shared Auth change was made by these historical probes.

**Release decision:** review publication and the original audit's hosted CI pass do not clear the remaining release gates. The later explicitly authorized, authenticated private draft import is recorded above; it does not activate or publish inventory. Do not promote `main`/Production, force launch or fabricate a review. Exact owner/operator steps are in [DEPLOYMENT.md](DEPLOYMENT.md).

## Windows real local-service browser workflow — historical 3 PASSED

The historical run of [../tests/integration/admin-workflow.spec.ts](../tests/integration/admin-workflow.spec.ts) covered the following **11 substeps at each of 390, 768 and 1440 px**. These were three complete workflow tests, not 33 independently counted tests; the new 12-substep scope is recorded above.

1. Reject an incorrect password, log in with a real local Auth account, check the HttpOnly session, rotate the actual refresh token/cookie and reload successfully; reject signed-out private routes/APIs.
2. Reject ordinary non-admin application login, establish a genuine ordinary Auth session for direct denial probes, check allowlist/RLS/preview-RPC denial and refuse any preexisting Chapra.
3. Preview the actual local **draft Hazaribag seed** through shared `CityDiscovery`, without editing/importing venues; its public URL remains 404.
4. Use the authenticated **Bihar-filtered GeoNames picker** to select Chapra (`geonames:1274353`, Saran), save a draft through the UI and verify `metadata.geographic_source_id`.
5. Add the associated venue through the workspace UI; save facts, facilities and private research atomically as an unreviewed draft, then inspect the city preview.
6. Reject SVG/oversized uploads through both UI and authorized API; upload actual generated **PNG, JPEG and WebP bytes**, verify stored WebP variants/private previews, and deny anonymous raw Storage/draft media and non-admin media operations.
7. Select the second cover, move photos both directions, edit alt text/credit, delete a photo and drain its durable Storage job through the admin UI.
8. Inspect saved venue/city previews, explicitly review/publish the venue, retain unverified status and prove it remains hidden while its city is draft; private research does not enter public output.
9. Activate the city deliberately; exercise fresh anonymous GET city/venue search, filters, price basis, sorting, facts, contact hrefs, gallery keyboard behavior, public images, canonicals and sitemap-eligibility RPCs. No phone call or WhatsApp navigation occurs.
10. Unpublish and verify disappearance, republish, then deactivate the city and prove its still-published venue is hidden from discovery, REST, media and sitemap eligibility.
11. Delete only the run-owned unpublished venue/inactive city through the UI, remove all remaining image variants via cleanup, preserve Hazaribag, sign out and verify session removal/private denial.

**Isolation:** Next uses `http://localhost:3200`; real local Supabase API uses `http://127.0.0.1:55321`, database **55322**. Fixtures remain false. The runner owns only `shagun-integration`, verifies container/ownership before reset and creates random local Auth identities. Allowed requests reach actual services unchanged; there are no Auth mocks or fabricated JWTs. Credentials stay in memory/child environments, with sensitive screenshots, traces, state files and reports disabled.

With CLI **2.116.0**, local `auth.email.enable_signup=true` enables the email/password provider; global `auth.enable_signup=false` rejects public registration, confirmed by a direct signup-denial assertion. These are **local test settings**, not a recommendation to modify shared Auth.

The suite uses one worker, no retries and test-owned Next server lifecycle. The first-use reset guard checks all schemas/bucket/policy targets before recording ownership; both source tests pass in the full check. Signed-in dashboard, city picker, venue editor, photo manager and public-layout previews pass axe and page-overflow checks at all three widths. An absolutely positioned screen-reader label had escaped its table scroller; **`position: relative` on `.a-table-wrap`** fixes the cause without hiding page overflow or weakening assertions. The **2.4-minute, 3/3 pass is after that fix and those checks**. See [../src/app/admin/admin.css](../src/app/admin/admin.css), [../scripts/integration.ts](../scripts/integration.ts), [../playwright.integration.config.ts](../playwright.integration.config.ts).

**Timing:** this Windows run **preceded the request-time sitemap refactor**. Separate Node 24 hosted runs passed **3 workflows in 1.9 minutes at `2072dc7`** and **3 workflows in 1.8 minutes at `9db51bd`**, after the sitemap refactor and, for the latter, the catalog guard. Those runs add real isolated-service evidence for their revisions, not managed-project acceptance.

**SEO boundary:** localhost HTTP is intentionally non-indexable. Canonicals and anonymous `sitemap_entries` eligibility were checked, while robots disallowed crawling and sitemap requests returned 404. This is not verification of production HTTPS sitemap XML, secure cookies or CDN behavior.

**Historical local integration shutdown:** `npm run integration:stop` succeeded at that checkpoint; only the unrelated `biharibhojan-db` container remained running and local test data was retained. This is not the current process state; use the latest restoration label above.

## Unit/database tests versus service tests

`npm run check` covers local validation, authorization/action/route contracts, Sharp image handling, catalog/preview behavior and SQL migrations, grants, lifecycle, exact optimistic versions, upload reservations and cleanup. PGlite uses synthetic managed-schema scaffolding and a single connection; mocked route/action clients are not live Supabase. The separate workflow above adds actual local Auth/REST/Storage evidence, not managed-project certification.

The historical post-catalog-fix local **952-test / 14-file** run included **60 catalog tests**, **19 request-time sitemap tests** and both reset guards; the current check is recorded separately above. The original hosted audit ran **950 tests / 14 files before the two catalog regressions were added**; these are supplied execution counts, not inferred totals. The malformed-ZIP advisory in the city-import dependency was fixed by pinning `fflate` **0.8.3**; the subsequent package-manager audit reported **zero vulnerabilities**. Supabase CLI remains **2.116.0**.

### Request-time sitemap regression coverage — 19 tests

- [../src/lib/sitemaps.ts](../src/lib/sitemaps.ts), [../src/app/sitemap.xml/route.ts](../src/app/sitemap.xml/route.ts) and [../src/app/sitemap/[partition]/route.ts](../src/app/sitemap/[partition]/route.ts) replace build-time `generateSitemaps` enumeration. Builds no longer fetch inventory to generate sitemaps; the request-time index grows new partitions without a rebuild and preserves `/sitemap/0.xml`.
- Tests cover index/partition boundaries, pagination and limits, XML escaping, noindex/local **404s**, invalid/private rows and sanitized no-store **503s** on invalid data or service failures. These are local contracts, not proof of managed HTTPS output.
- Async [../src/app/robots.ts](../src/app/robots.ts) makes **no database request** and advertises **one index URL**, `/sitemap.xml`, when indexing is enabled; it does not enumerate partition URLs.

## Windows public fixture matrix — all scenarios passed before the catalog guard

These supplied Windows Chromium results cover **all six configured widths and all three scenarios before the final catalog-only guard**. They are not the older single-width empty/one runs or a hosted all-scenario matrix; original audit CI ran only `many`.

| Scenario | Widths | Passed | Intentional skips |
| --- | --- | ---: | ---: |
| `many` | 320, 375, 390, 414, 768, 1440 px | 166 | 1 |
| `empty` | 320, 375, 390, 414, 768, 1440 px | 95 | 28 |
| `one` | 320, 375, 390, 414, 768, 1440 px | 120 | 21 |
| **Total** | All three scenarios, all six widths | **381** | **50** |

The many-scenario desktop skip excludes a mobile-only interaction; empty/one skips are inapplicable multi-record or absent-media cases, not waived failures. All existing assertions remain in place. These 381 fixture passes are separate from the three real local-service workflows; neither is a production-inventory certification.

An earlier local fixture Lighthouse run recorded **99 performance / 100 accessibility**; it is historical, not fresh or final-domain evidence.

Reproduction commands and the separate default-3000/fixture-3100/integration-3200 contracts are in [../README.md](../README.md). At that historical catalog checkpoint, the normal build passed with `SHAGUN_TEST_FIXTURES=false` and the browser at `http://localhost:3000/` confirmed the honest empty preparation state, with no synthetic warning or inventory. This does not establish the current build or preview state. Never reuse a mismatched build or weaken assertions to obtain a pass.

## Historical published review revisions and hosted evidence

[../.github/workflows/ci.yml](../.github/workflows/ci.yml) runs Node **22** quality checks/database-free public fixtures, then Node **24** real local Auth/publishing/Storage workflows on an isolated Docker-backed Supabase stack. The authenticated workflow suppresses credentials, Auth traces, populated form snapshots and database dumps; its stop step uses `always()` to clean up only that stack. No production credentials or managed test records are required.

**Historical review publication — SUCCESS:** [audit/second-pass-2026-09-09](https://github.com/RichardHenryJames/shagun/tree/audit/second-pass-2026-09-09) contains the published follow-up commit **`9db51bd5b0a11395d62401353fde3e0db03a59fe`**. It follows original audit commit **`2072dc7f8aba3e492d448c94d703450aa0312a75`**, whose local/GitHub branch SHA match was confirmed at its publication. Neither revision represents the new autocomplete changes; the later supplied `5a4003b` green checkpoint also predates them.

### Original audit hosted CI — SUCCESS at `2072dc7`

[Run 34320416797](https://github.com/RichardHenryJames/shagun/actions/runs/34320416797) **succeeded in both jobs**. Actual hosted logs confirmed:

| Job | Confirmed execution at `2072dc7` |
| --- | --- |
| Node **22** quality/public fixtures | Full check **950 tests / 14 files**; `many` fixture scenario **166 passed / 1 skipped**. No hosted `empty`/`one` matrix is claimed. |
| Node **24** real local Auth/Storage | Auth, Chapra/venue creation, media, publishing and cleanup: **3 passed in 1.9 minutes**, **after the sitemap refactor at this commit**. These are real isolated services hosted by CI, not managed production acceptance. |

This closes the original audit CI check. The later **catalog follow-up** has its own successful run in the historical `FOLLOW_UP_HOSTED_CI_RESULT` above, including the two new regressions and all three configured Auth workflows; that is not the current autocomplete CI result. Initial [run 34222982087](https://github.com/RichardHenryJames/shagun/actions/runs/34222982087), which passed for `2edd4fd` without fetched hosted counts, remains earlier history.

### Actual Vercel Preview smoke — `2072dc7`, one defect found

GitHub deployment **6344149398** records `environment: Preview`, **`production_environment: false`** and state **`success`** for `2072dc7`. The actual preview was [https://shagun-hs7en3zi0-richards-projects-224a1dea.vercel.app](https://shagun-hs7en3zi0-richards-projects-224a1dea.vercel.app).

| Actual preview surface | Observed response |
| --- | --- |
| `/` | Honest empty preparation state. |
| `/cities`, `/search` | **200**, noindex. |
| Missing city URL and `/city/hazaribag` | **404**, noindex. |
| `/admin/login` | **200**, noindex. |
| `/robots.txt` | **200**, disallow all. |
| `/sitemap.xml` | **404**. |
| Unconfigured `/api/admin/city-catalog` | Sanitized **500**, no leak; this actual smoke finding led to the later fix. |

**This was not an all-green preview smoke.** Deployment state `success` did not negate the catalog 500. These observations remain scoped to `2072dc7`; the corrected catalog response below was checked locally, not retroactively verified on this old preview.

### Catalog follow-up — `9db51bd`, local and hosted checks passed

The published fix adds an **`isConfigured` guard before client construction**: a missing Supabase URL or public key returns **503**, private/no-store and noindex, with no catalog data. The configured path retains fresh managed Auth and active-UUID allowlist checks unchanged. Two new unit cases cover missing URL and missing key; no SQL or shared settings changed. This corrects an unconfigured response-status defect, not a demonstrated data leak or Auth bypass.

After the fix, the full local check and normal fixtures-false build passed as recorded above. Actual localhost:3000 catalog requests with **valid and malformed queries** both returned **503**, `Cache-Control: private, no-store`, noindex and the **exact asserted non-secret error message**, with no catalog data. The normal empty home was restored and confirmed. The final-code hosted pass was independently confirmed through run metadata and the exact log totals above.

GitHub deployment **6344431243** records `environment: Preview`, **`production_environment: false`** and state **`success`** for `9db51bd`, at [https://shagun-ykfvegpke-richards-projects-224a1dea.vercel.app](https://shagun-ykfvegpke-richards-projects-224a1dea.vercel.app). Its actual browser smoke passed all asserted responses: empty home; cities/search and admin login **200/noindex**; missing city and Hazaribag **404/noindex**; sitemap **404/noindex**; robots **200/disallow all**; and both valid/malformed catalog queries **503/private/no-store/noindex**, containing only the exact non-secret setup error and no catalog data. This confirms the original preview defect is fixed on the new deployment. It does not establish a configured managed admin session or production inventory behavior.

### Historical observed promotion boundary

At that observation, the GitHub `main` API confirmed the remote branch unchanged at **`b62d6eda5fde1fe5e588ef3a4b2a5cdeb4693908`** and the user's local `main` at **`b066ed5`**. These are historical SHAs, not a fresh branch query. Only the recorded **Preview/non-Production** deployment is established; no production promotion occurred. The Vercel production-branch setting itself was not fetched, so these observations are not a global provider-setting claim. **Do not promote `main` or Production.**

## Remaining evidence required

- **Build/preview and CI scope:** the recorded labels above are filled for their exact revisions/environments. Repeat applicable checks after code/configuration changes, not merely because of this documentation update. A historical process restoration is not current process state; never report the failed parent run's real-Auth job as passed.
- The owner-authorized two-table Bihari repair is applied, with scoped preservation, role denial and backend checks recorded above. Preserve that repair; monitor subsequent permission changes and do not treat it as full recovery, every possible indirect-path test or automatic release approval.
- The Shagun grant contract is restored and the standard read-only inspection passes. Complete the remaining managed ordinary/admin/revoked-session REST/RPC/Storage checks; existing anonymous grant-level denials are no longer blocked on remediation. Do not re-add the exposed `shagun` schema or weaken RLS for acceptance tests.
- The owner has signed in privately on the short origin, all 12 drafts are imported and explicitly reviewed, and removal of the extra hosting login is complete. Complete the remaining managed refresh/expiry/recovery/revocation checks; do not repeat provisioning, the hosting exception or approval for unchanged saved facts. Preserve unknowns/source caveats and require new review after factual edits. Missing image rights require empty photo collections, not fabricated photos, and do not alone bar an approved text-only listing. Never request a password in chat or fabricate a session.
- **Current-code hosted evidence and managed HTTPS acceptance:** earlier catalog CI/Preview passes do not cover the published autocomplete/harness revisions. Preserve the current local repeats/`many`/post-sitemap owned-Auth scope and the earlier `empty`/`one` matrix history; do not call those scenarios rerun. Owner-gated managed acceptance still needs HTTPS inventory metadata/sitemaps, secure cookies, image caching, six-width accessibility and final-domain performance; unconfigured or Vercel-protected Preview access is not production acceptance.
- Multiple real concurrent editor sessions, lock/quota/finalization races and managed-provider partial-upload/cleanup failure paths.
- The new encrypted root backup and SQL restore cover current Shagun rows, ledger, permissions and routine definitions. Complete independent off-device/key recovery and supported managed Auth/settings, object-service and deployment-configuration recovery separately; the local Windows-profile-bound artifact does not supply them. The Free plan lacks scheduled project backups, but no paid upgrade is mandatory or authorised. Neither the old public-only nor the new SQL-only restore is full managed recovery.

[DEPLOYMENT.md](DEPLOYMENT.md) defines closure criteria. No managed Hazaribag launch, complete recovery or universal security/accessibility certification follows from local passes.